<% if logged_in? %>
  <p>ログイン完了ユーザ: <%= current_user.name %></p>
  <%= link_to 'タスクリストを表示する', tasks_url, class: 'btn btn-lg btn-primary' %>
<% else %>
    <div class="center jumbotron">
      <div class="text-center">
        <h1>タスクリスト！！</h1>
        <%= link_to '新規登録', signup_path, class: 'btn btn-lg btn-primary' %>
      </div>
    </div>
<% end %>